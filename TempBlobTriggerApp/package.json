{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "local": "npm run build && func start"
  },
  "dependencies": {
    "@azure/arm-containerinstance": "^9.1.0",
    "@azure/functions": "^4.12.0",
    "@azure/identity": "^4.13.1",
    "dotenv": "^17.4.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.2"
  }
}
